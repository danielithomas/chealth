This project is a web application built with React and Node.js.
It uses PostgreSQL for the database and Redis for caching.
The frontend communicates with the backend via REST APIs.
Authentication is handled by JWT tokens stored in cookies.
The deployment pipeline uses GitHub Actions for CI/CD.
Docker containers are used for local development and production.
Environment variables are managed through .env files.
The project follows a monorepo structure with multiple packages.
